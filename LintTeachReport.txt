ByzMetaTeacher lint repair record
================================

Previous report status:
  The prior ERR6002 report was generated against the pre-repair source and is retained here only as historical evidence.

Repairs applied:
  1. Removed accidental Markdown triple-backtick fences from .per source.
  2. Split initialization into compact one-shot state/configuration rules.
  3. Added compact enemy-threat observation goals.
  4. Replaced repeated nested enemy OR trees in strategy and unit-goal consumers with state-goal consumers.
  5. Replaced repeated five-unit cavalry OR trees in Barracks capacity consumers with a stored cavalry-mass level and up-compare-goal.
  6. Preserved the teaching pipeline: observation -> strategy -> unit-goal -> production capability -> training.

Structural verification of current source:
  rules=80
  markdown_fences=0
  rules_over_32_elements=0
  oversized_rules=none

Community basis:
  - AoE2 DE rule length is limited to 32 facts/actions/logical-operator elements.
  - Community scripts use state goals to avoid repeating large condition trees and explicitly document split-rule goals for long rules.
  - Goal comparisons use goal-typed comparison operations such as up-compare-goal.

This file is a repair record, not a substitute for running the installed AoE2 AI linter/editor against the current checkout.
