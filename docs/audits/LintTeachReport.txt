ByzMetaTeacher lint repair record
================================

Forensic baseline
-----------------
The historical ERR6002 report in commit 12e29502cceb40c6e584f7e227e80241afc0b5d2 contains 210 diagnostic entries. Those entries are not 210 independent oversized rules.

The historical source has a malformed initialization rule beginning at line 172. It contains 23 runtime actions plus its fact, and it also contains literal Markdown fence lines. That rule is unambiguously over the community-compatible 16-element limit and over DE's 32-element limit once its commands/operators are counted. The Markdown fences are also invalid .per source.

The remaining ERR6002 line ranges in that historical report map, in the same repository snapshot, to rules whose actual command counts are below both 16 and 32. They therefore cannot be treated as independently verified rule-too-long defects from that snapshot. They are retained as historical diagnostics, not as a list of 210 separate code repairs.

Community standard used
-----------------------
1. AIRef documents 32 elements per rule in DE and 16 in UP; the defrule and => separator do not count. Elements include commands and logical operators.
2. Established community practice is to keep rules comfortably below the DE ceiling, and often at or below 16 when practical, using state goals or split rules rather than giant condition trees.
3. Binary logical operators such as and/or take two direct child facts. Larger OR sets must be nested or, where appropriate, represented as state goals.
4. Goals are used as state variables to reduce repeated condition trees and keep strategy/production consumers readable.

Repairs
-------
1. The oversized initialization rule was split into three one-shot initialization rules. This preserves the teaching architecture while reducing the command load.
2. All accidental Markdown fences were removed from .per source.
3. The existing threat-state architecture was retained rather than replacing it with a new controller.
4. Every multi-child OR introduced by the prior repair was converted to binary nested OR form. No n-ary direct OR remains.
5. The strategy -> unit-goal -> production -> training teaching pipeline remains intact.

Current source audit
--------------------
Source: ByzTeacher/ByzMetaTeacher.per
Rules: 80
Maximum counted rule elements: 13
Rules over 16 elements: 0
Rules over 32 elements: 0
N-ary logical operators: 0
Source lines over 255 characters: 0
Markdown fences in source: 0

Important limitation
--------------------
This is a repository-source audit. It does not claim that an arbitrary older file already downloaded to an AoE2 installation has been replaced. The installed file must be the current main-branch source containing the repair commit.

Recent repair commit
--------------------
803cc3a28fd38907329b801d9e0e7cbe9d1e30c9
Fix logical-operator arity using nested community-style ORs

This report intentionally distinguishes verified source defects from stale or cascading historical diagnostics rather than declaring every historical ERR6002 line to be a separate programming error.


Current debugger audit (main after core repair)
-----------------------------------------------
Source commit: 7b4060eb946c923a40811914cd071c8da9bff7eb

Verified source invariants:
- 95 defrule forms
- 95 => separators
- balanced parentheses
- no nested defrule forms
- no rule closes before its =>
- no rule remains open after its action block
- no direct n-ary logical operators
- no source line exceeds 255 characters
- no remaining goal ... != ... predicates
- counter goals 42, 43, and 44 are explicitly initialized before their threat-state consumers run

Fatal source defects repaired:
1. The Pikeman research rule had an extra close before its => and therefore did not form a valid rule. The close was moved to the end of the action block.
2. The two goal-negation predicates using "goal ... != ..." were replaced with documented (not (goal ... ...)) facts.
3. Threat counter goals 42/43/44 were added to startup initialization rather than relying only on the per-pass threat reset.

Semantic limitations that remain intentionally unclaimed:
- This is a source-structure audit, not proof that the installed AoE2DE parser accepts the file.
- Runtime behavior is not proven by repository inspection.
- Strategic number 191 is retained as a community-established resource-control convention, but its collision status should be verified against the exact target DE build before treating it as a formally reserved custom SN.
- Current source still requires gameplay validation for same-pass state visibility, research/production arbitration, building placement behavior, scouting behavior, and attack-group lifecycle.

Reference standard:
AIRef currently documents 32 elements per DE rule, binary logical operators, goal variables, escrow-aware commands, queued-unit semantics for unit-type-count-total, and explicit initialization as established defensive practice. These facts are used as engine/reference constraints; gameplay architecture decisions remain controller-specific.
